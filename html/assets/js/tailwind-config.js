tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: '#db2777',   // Pink-600 (School color)
                primaryHover: '#be185d', // Pink-700
                secondary: '#0ea5e9', // Light Blue-500 (School color)
                secondaryHover: '#0284c7', // Light Blue-600
                accent: '#fdf2f8',    // Pink-50 (bg)
                dark: '#1e293b',      // Slate-800
                light: '#f8fafc'      // Slate-50
            },
            fontFamily: {
                sans: ['Prompt', 'sans-serif'],
                heading: ['Outfit', 'sans-serif']
            }
        }
    }
}
